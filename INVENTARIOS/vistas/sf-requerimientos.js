/* INVENTARIOS · GI-23 requerimientos de materia prima — HTML
   Los paneles están en vistas/solicitudes-fabricacion.js; el cálculo sale de Explosion (base compartida) y no admite
   materiales manuales por línea (la variante se define eligiendo otra lista de materiales, tipo Especial). */
